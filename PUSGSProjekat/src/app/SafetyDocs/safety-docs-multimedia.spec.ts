import { SafetyDocsMultimedia } from './safety-docs-multimedia';

describe('SafetDocsMultimedia', () => {
  it('should create an instance', () => {
    expect(new SafetyDocsMultimedia()).toBeTruthy();
  });
});
