import { SafetyDocsChecklist } from './safety-docs-checklist';

describe('SafetDocsChecklist', () => {
  it('should create an instance', () => {
    expect(new SafetyDocsChecklist()).toBeTruthy();
  });
});
