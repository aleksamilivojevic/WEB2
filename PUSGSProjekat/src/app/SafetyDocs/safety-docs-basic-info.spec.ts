import { SafetyDocsBasicInfo } from './safety-docs-basic-info';

describe('SafetDocsBasicInfo', () => {
  it('should create an instance', () => {
    expect(new SafetyDocsBasicInfo()).toBeTruthy();
  });
});
